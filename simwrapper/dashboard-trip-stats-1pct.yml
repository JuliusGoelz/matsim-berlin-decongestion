header:
  title: Trip stats

layout:
  rowTripDuration: # contains one map per run with the capacity usage
    - title: 'Trip duration'
      description: 'Comparing trip duration between base, decongestion and roadpricing scenarios'
      type: 'bar'
      # width: 3
      dataset: 'src/main/python/output/trip_stats.csv'
      x: 'mode'
      columns: ['basecase', 'decongestion', 'roadpricing']
      xAxisName: 'Mode'
      yAxisName: 'Average trip duration [min]'